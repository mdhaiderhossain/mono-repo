{
    "name": "booking-system",
    "version": "1.0.0",
    "description": "",
    "workspaces": [
        "apps/*"
    ],
    "scripts": {
        "dev": "concurrently \"npm run dev --workspace=booking-backend\" \"npm run dev --workspace=booking-client\"",
        "build": "npm run build --workspace=booking-backend && npm run build --workspace=booking-client",
        "start": "concurrently \"npm run develop --workspace=booking-backend\" \"npm run dev --workspace=booking-client\""
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}
